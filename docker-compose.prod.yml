# Production override for Inky Frame
# Use: docker compose -f docker-compose.yml -f docker-compose.prod.yml up

services:
  inky-frame:
    command: ["gunicorn", "--bind", "0.0.0.0:5000", "--workers", "2", "--threads", "4", "app:app"]
    environment:
      - PYTHONPATH=/app